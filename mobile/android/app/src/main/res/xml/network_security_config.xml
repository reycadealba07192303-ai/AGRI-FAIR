<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9+ blocks plain http, and the dev backend has no certificate. Only
  the hosts below are opened up; everything else still requires https.

  These are IP addresses, not domains, so each one has to be listed in full -
  `includeSubdomains` does nothing for an IP, and a partial address like
  192.168.0.0 matches nothing at all.

  When the laptop's DHCP lease changes, add the new address here as well as in
  lib/services/api_config.dart. Delete this file once the backend is on https.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator's route to the host machine -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>

        <!-- This laptop on Wi-Fi, for running on a real phone -->
        <domain includeSubdomains="false">192.168.254.114</domain>
    </domain-config>
</network-security-config>
